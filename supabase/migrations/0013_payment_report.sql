-- ----------------------------------------------------------------------------
-- 고객이 /billing 페이지에서 "결제 완료 신고" 누른 시각.
-- 슈퍼관리자 어드민에서 토스 확인 → 활성화 흐름의 트리거 기록.
-- ----------------------------------------------------------------------------
alter table public.subscriptions
  add column if not exists last_payment_reported_at timestamptz;
